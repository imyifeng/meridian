# Meridian

Meridian 是一款自托管的备忘录工具：以稳定的传统备忘录功能为基底，叠加对话式 AI 助手。一个实例内有多个账号；除分类体系外，一切数据按账号隔离。

## Language

### 账号与部署

**User（用户）**:
Meridian 实例上的一个账号。除分类体系外的所有数据（备忘录、标签、提醒）都归属于单个用户，用户之间彼此不可见。
_Avoid_: 账户、member、account

**Administrator（管理员）**:
拥有管理职责的用户：经由 Web 管理控制台管理用户、分类体系与 AI 设置，是唯一能增减分类的角色。
_Avoid_: admin、超管、owner

**Instance（实例）**:
一次自托管部署所运行的完整 Meridian 服务及其自带数据库；分类体系与账号都属于实例。
_Avoid_: server（保留给服务端进程/机器）、站点

**AI Settings（AI 设置）**:
实例级唯一的大模型接入配置（Base URL、模型名、API Key 与启用开关），仅管理员可在 Web Console 中修改与测试；配置生效后智能体对所有用户可用。
_Avoid_: 模型配置、LLM 配置

**Setup Wizard（初始化向导）**:
未初始化实例的唯一入口流程：创建首个管理员。完成后自助注册永久关闭。
_Avoid_: 注册页、首个注册用户

**Trial Build（试用构建）**:
在自有环境本地构建、部署与试用的产品形态，产物不对外分发；Meridian 当前只有这一种形态。
_Avoid_: 试用版、内测版、本地构建（作为名词时）

### 核心概念

**Memo（备忘录）**:
Meridian 的核心记录单元：归属于一个用户、存放在恰好一个分类下，由纯文本标题与纯文本正文构成。
_Avoid_: note、entry、便签（见 Tag）

**Category（分类）**:
实例级固定分类体系中的一员，仅管理员可增减；每条备忘录恰好归入一个分类，归入关系可更改。客户端与 AI 都只能使用既有分类，不能创建或删除。体系内含系统内置的未分类（见 Uncategorized）。
_Avoid_: folder、笔记本、group

**Uncategorized（未分类）**:
系统内置分类：新备忘录的默认归属，也是分类被删除时其备忘录的兜底去处；不可删除、不可改名。
_Avoid_: 默认分类、none

**Tag（标签）**:
用户自由添加、依附于备忘录的短文本整理线索，可据其检索备忘录；不跨用户共享，永远不会由 AI 添加或修改。
_Avoid_: 便签（早期需求文档的误称，实际指本词）、sticky、label

**Reminder（提醒）**:
附着在备忘录上的到期时间：一次性时间点，或按固定模式循环（每天、每周某日、每月某日、每年，可带间隔）；到期时所有已登录客户端在应用运行期间触发本地系统通知；实例不做推送。
_Avoid_: 闹钟、推送

**Recycle Bin（回收站）**:
被删除的备忘录的去处：其中的 Memo 不出现在正常列表与搜索里，可恢复回原分类，也可彻底删除；永不自动清空，彻底删除不可撤销。标签不设自己的回收站。
_Avoid_: 垃圾桶、废纸篓、bin

### 智能体

**Agent（智能体）**:
客户端内置的对话式助手：用户以自然语言托其创建、检索、修改与删除备忘录；每个用户一个常驻会话。除标签外的一切备忘录操作皆可经其完成。
_Avoid_: 聊天机器人、bot、ChatGPT

**Draft（草稿卡片）**:
智能体在会话中提出的备忘录预览：本质是微缩的创建界面，各字段（分类、提醒等）可手动调整，经用户确认后才真正创建；标签永远为空、只能由用户手动添加。
_Avoid_: 确认消息、预览卡

**Conversation（会话）**:
智能体页面的单一常驻对话及其聊天记录，归属单个用户；只能被用户整段删除，无多会话并存。
_Avoid_: 聊天室、thread

### 界面

**Client（客户端）**:
用户日常使用的 Windows / Android 应用；分类体系在其中只读。
_Avoid_: app（行文可用）、原生端

**Web Console（Web 管理控制台）**:
管理员在浏览器中使用的管理界面，用于管理用户与分类体系。
_Avoid_: 后台、admin panel

**Web Client（Web 简易客户端）**:
普通用户在浏览器中临时查看与简单编辑备忘录的轻量界面；不提供提醒通知。
_Avoid_: 网页版、web app

**Theme（主题）**:
客户端的深浅外观偏好：深色、浅色或跟随系统，默认跟随系统；存于客户端本地，不入账号数据。
_Avoid_: 深色模式（指具体选项时）、外观
