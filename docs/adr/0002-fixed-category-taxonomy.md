# 分类体系仅管理员可变，每条备忘录单一分类

分类体系是实例级的固定集合：只有管理员能在 Web 管理控制台增减，客户端与 AI 一律只读。每条备忘录恰好归入一个分类（一个分类可含多条备忘录），归入关系可以更改。这样设计的根本原因是给未来的 AI 自动分类划出边界：AI 只能在既有分类中做单选，既不能自创分类，也不能把一条备忘录归入多类，从数据模型上杜绝 AI 滥生分类。

体系内含系统内置的"未分类"分类：新建备忘录默认归入未分类；管理员删除分类时，其下备忘录全部流入未分类，不做强制迁移。
