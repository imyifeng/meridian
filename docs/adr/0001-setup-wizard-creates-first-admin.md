# 首个管理员经初始化向导创建，而非开放注册

需求原文是"首个注册用户自动成为管理员"，但公网部署时开放注册存在抢注窗口：实例初始化前，任何陌生人都能抢先注册成为管理员。因此实例首次启动进入未初始化状态，唯一可用的操作是初始化向导（创建管理员）；完成后自助注册永久关闭，后续用户一律由管理员在 Web 管理控制台手动创建。

## Considered Options

- 开放注册至首个用户出现（需求字面）：有公网抢注风险，否决
- 仅允许本机/内网请求注册首个管理员：可用但交互别扭，否决
- 初始化向导（采纳）：功能等价且消除抢注窗口
