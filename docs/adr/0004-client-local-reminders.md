# 提醒只由客户端本地调度，不做服务端推送与后台保活

提醒到期时由客户端在应用运行期间触发本地系统通知（Windows 通知 / Android 通知）。服务端不做推送（Android 需 FCM、Windows 需 WNS，都与自托管轻量部署冲突），也不做后台保活。明确接受的限制：应用未运行时提醒不触发。这是刻意的边界，不是缺陷，不要"顺手修复"。v1 只做一次性提醒（重复提醒留待后续）；提醒是备忘录的属性而非设备的属性，到期时所有已登录客户端都会触发。
